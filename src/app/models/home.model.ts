export interface PropertyTypeStats {
    apartamento: number;
    casa: number;
    terreno: number;
}


export interface PropertyValueStats {
    min: number | null;
    average: number | null;
    max: number | null;
}